
export type Themes = {
  "mal_id": number,
  "type": string,
  "name": string
}

export type Pagination = {
  "last_visible_page": number,
  "has_next_page": boolean,
  "current_page": number
}